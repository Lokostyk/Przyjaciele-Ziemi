import React from 'react'
import { MapContainer, TileLayer } from 'react-leaflet'

function Map(props) {
    return (
        <MapContainer>
            <TileLayer />            
        </MapContainer>
    )
}

export default Map
